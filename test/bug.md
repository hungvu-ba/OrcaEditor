### Sửa lỗi:

1.  khi select text và chọn format code thì selected text bị xoá -> kỳ vọng là selected text được đưa nguyên vào trong code block.
2.  khi select text và bấm để tạo link hoặc link tới ảnh thì behaviour đang là bay thẳng về vị trí đầu tiên của file -> cần giữ ở vị trí hiện tại vừa select.
3.  Cmd + V đang không paste được

### New feature:

1.  enter sẽ check để format bullet nếu các dòng trước và sau chỗ enter có định dạng phù hợp.
2.  add auto fỏmat bullet points in table cells
