1.  [x]  mất hover tool tip khi hover ở các icon ở toolbar
2.  [x]  icon code block không hoạt động đúng khi select 1 đoạn text ở giữa câu rồi chọn code block. Kỳ vọng sẽ xuống dòng ở chỗ bắt đầu chọn text, convert selected text thành code block. xuống dòng tiêp rồi mới đến đoạn text cuối câu.
3.  [x]  icon paragraph cạnh H3 không hoạt động đúng khi undo ko undo lại từng hoạt động mà reset format.
4.  [x]  tooltip "đoạn văn thường" của icon số 8 gây hiểu nhầm.
5.  [x]  dịch tooltip sang EN.
6.  [ ]  select 1 bullet paragraph gồm nhiều item và chọn loại bullet khác hoặc chọn checkbox thì cần convert toàn bộ các dòng sang loại mới được chọn, lưu history để undo được đúng. Giờ mới chuyển đổi đúng giữa 2 loại bullet. chuyển sang checkbox bị lỗi.
7.  [x]  khi width của tab nhỏ hơn 1/2 widtd màn hình thì ẩn TOC. user có thể tự bật lại được nếu muốn.
8.  [ ]  thêm tính năng report lỗi lên thẳng github. - later
